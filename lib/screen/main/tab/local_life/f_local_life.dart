import 'package:flutter/material.dart';

class LocalLifeFragment extends StatelessWidget {
  const LocalLifeFragment({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
